[task]
id = "fare-rules-123"
domain = "fare_rules"
skill = "fare-rules"
verifier = "tool_call_check"
weight = 1.0

[expected]
tools = ["get_fare_rules", "search_flights"]
required_params = { get_fare_rules = ["flight_id"], search_flights = ["date", "destination", "origin", "passengers"] }
